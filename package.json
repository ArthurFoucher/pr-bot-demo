{
  "name": "pr-bot-demo",
  "version": "1.0.0",
  "scripts": {
    "lint": "jshint hello.js hello.test.js",
    "test": "npm run lint && node ./hello.test.js"
  },
  "devDependencies": {
    "jshint": "^2.9.5",
    "pr-bot": "^0.2.0",
    "tape": "^4.2.2"
  }
}
